// Create Event for obj_chat_window

// Initialize variables for chat
chat_text = "";       // Text to be displayed in the chat window
choices = [];         // Array to store player choices
choice_selected = 0;  // Index of the currently selected choice

// Ensure global.chat_window is properly initialized
if (!variable_global_exists("chat_window")) {
    global.chat_window = noone; // Initialize the global variable if it doesn't exist
}

// Prevent duplicate instances
if (global.chat_window != noone && instance_exists(global.chat_window)) {
    instance_destroy(); // Destroy this instance if another chat window already exists
} else {
    global.chat_window = id; // Assign this instance to the global variable
}
