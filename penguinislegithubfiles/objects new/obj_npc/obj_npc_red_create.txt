// Create Event for obj_npc_red

// Initialize the animation index
animation_index = 0;

// Define animations for npc_red
animations = [spr_red_lookaround, spr_red_talk];

// Set the initial sprite
sprite_index = animations[animation_index];