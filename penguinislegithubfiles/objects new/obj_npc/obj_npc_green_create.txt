// Create Event for obj_npc_green

// Initialize the animation index
animation_index = 0;

// Define animations for npc_green
animations = [spr_green_lookaround, spr_green_talk];

// Set the initial sprite
sprite_index = animations[animation_index];